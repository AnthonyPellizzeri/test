pipeline {
    agent any
    stages {
        stage('Build testtttt') {
            steps {
                sh 'echo "Hello World"'
                git 'https://github.com/foo/bar.git'
            }
        }
        
    }
}
